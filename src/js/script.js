// /* SOLO PER HOME PAGE */
//   document.addEventListener('DOMContentLoaded', function () {
//     const header = document.querySelector('.hero__inner');
//     const logo = document.querySelector('.hero__inner__img');
//     const links = document.querySelectorAll('.hero__menu a');
//     const icons = document.querySelectorAll('.hero__icon a');
  
//     function toggleHeaderStyle() {
//       if (window.scrollY > 50) {
//         header.classList.add('scrolled');
//         logo.style.filter = 'none';
//         links.forEach(link => link.style.color = 'black');
//         icons.forEach(icon => icon.style.filter = 'none');
//       } else {
//         header.classList.remove('scrolled');
//         logo.style.filter = 'invert(100%)';
//         links.forEach(link => link.style.color = 'white');
//         icons.forEach(icon => icon.style.filter = 'invert(100%)');
//       }
//     }
  
//     // Ascolta l'evento di scroll
//     window.addEventListener('scroll', toggleHeaderStyle);
  
//     // Chiamata iniziale per impostare lo stile in base alla posizione dello scroll al caricamento della pagina
//     toggleHeaderStyle();
//   });
  

// Aggiungi questo script nel tuo file JS, o tra i tag <script> nella tua pagina HTML

